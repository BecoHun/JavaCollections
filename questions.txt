1.
Which of the following statements regarding the Set<E> interface are true?
Set<E> extends the Collection<E> interface.
Set<E> does not allow duplicate elements.
All methods defined in Set<E> are also defined in Collection<E>.

2.
Which of the following collections do not accept duplicates?
Set
SortedSet
The absence of duplicates is supported by all types of sets.

3.
Which of the following classes returns unique elements in sorted order?
TreeSet
The uniqueness of elements and sorting are basic priorities of tree sets.

4.
Which of the code fragments will compile successfully, given the class description below?
class Old {
    public static Object get(Set set) {
        return set.toArray();
    }
}
Object o = Old.get(new LinkedHashSet());
Object o = Old.get(new LinkedHashSet <Object>());
String[] s = (String[])Old.get(new LinkedHashSet<String>());
The toArray() method converts a collection into an array.
The collection may or may not be parameterized.
The array element type corresponds to the collection element type.
The get() method of the Old class returns an array via the Object supertype—polymorphism comes into play here.
The return value can be cast to an array again, given the type of elements.

5.
Which of the following statements about maps are true?
A map is a set of keys associated with a set of objects.
Keys cannot have duplicates.
A map is a set of key-value pairs in which keys identify values/objects. Keys must be unique, and only one value/object can be associated with a key.

6.
Which of the following methods are defined in the Map<K,V> interface?
Collection<V> values()
void clear()
V get(Object k)
The values(), clear(), and get() methods are described in the Map interface.
The put() method is used to add elements to the map, not the add() method. And the map's remove() method returns the object to be removed, not a Boolean value.

7.
What will be the result of running the following code fragment?
Map<Integer,String> map = new HashMap<>();
map.put(1, "one");
map.put(2, "two");
map.put(3, "three");
for (Map.Entry<Integer, String> elem : map.entrySet()){ 
    System.out.print(elem.getKey() + ":" + elem.getValue() + "; ");
}
Compilation is successful, and the code runs fine.
The map is created, and elements are added to it successfully.
The entrySet() method of the Map interface returns a set of key-value entries of the Map.
Entry type, so this set can be iterated with a for-each loop.
The getKey() and getValue() methods of the Map. Entry interface allow you to get the key and the object of the current entry, respectively.


8.
Which classes support iteration of elements in insertion order?
LinkedHashSet
LinkedHashMap
Yes, these classes implement a hash table and have a linked list structure.
This default list captures the elements in the order they were added.

9.
What will be the result of running the following code fragment?
import java.util.HashMap;
import java.util.Map;

class Task {
    private String name;
    Task(String name) {
        this.name = name;
    }
    public boolean equals(Object o) {
        return ( (Task)o).name == this.name;
    }
    //  public int hashCode() {    return 42;    }
}
class MapDemo {
    public static void main(String[] args) {
        Map <Task, String> map = new HashMap<>();
        Task task = new Task("pay");
        Task task1 = new Task("pay");
        Task task2 = new Task("job");
        map.put(task, "shop");
        map.put(task1, "bill");
        map.put(task2, "clean");
        System.out.println(map.size());
    }
}
A collection size of 3
If you uncomment the hashCode() method, the collection size will be 2.
When the hashCode() method of the Task class is commented out, the implementation inherited from the Object class is used.
In this case, the hash code is defined as the address in the memory where the object is located.
Therefore, objects task and task1 will be hashed into different buckets, and both will be added to the collection.
When the hashCode() method is uncommented, task and task1 are hashed into the same bucket, and a collision occurs.
When the collision is resolved, they are defined as the same key, and the value is replaced by the key.

10.
What will be the result of running the following code fragment?
Map<Integer, String> map = new LinkedHashMap<>(); 
map.put(1, "one"); 
map.put(2, "two"); 
map.put(3, "three"); 
map.put(1, "newOne"); 
System.out.println(map);
{1=newOne, 2=two, 3=three}
By default, the list in the LinkedHashMap is maintained in the order in which pairs are added.
However, if the key already exists, the value is replaced, and the order of the elements in the list does not change.

11.
Which statements about the SortedMap<K,V> and NavigableMap<K,V> interfaces are true?
The NavigableMap interface allows you to find pairs by closest matches for given keys.
The SortedMap<K,V> interface sorts keys contained in a map.
SortedMap orders keys naturally using Comparator.
The Navigable interface further searches for elements by the closest matches for given keys.
The HashMap class does not implement the NavigableMap interface because it does not store objects in order.
The firstKey() method returns the key with the smallest value.

12.
What will be the result of the code given below?
import java.util.*;
public class Quest {
    public static void main(String[] args) {
        SortedMap<String, Number> nmap = new TreeMap<>();
        nmap.put("one", 1); 
        nmap.put("two", 2);
        nmap.put("three", 3); 
        nmap.put("four", 4);
        Map<String, Number> map = nmap.headMap("three");
        System.out.println(map);
    }
}
{four=4, one=1}
The tree map is a tree sorted by key in natural order by key.
Its headMap method returns a map only with keys less than the specified one, thus yielding pairs with the keys "one" and "four."

13.
What will be the result of the following code fragment?
Map<Integer, Integer> map = new TreeMap<>();
map.put(1,1);
map.put(0,10);
map.put(null,0);
System.out.println(map);
A runtime error of the NullPointerException
The key cannot be null because, when added to an ordered tree map, the values of the keys are compared.

14.
Which of the following is the correct way to declare the variable of the EnumMap type given in the code fragment below?
enum Type {
    ONE, TWO
}
EnumMap<Type, String> enumMap1 = new EnumMap<>(Type.class);
To create a new empty enum map, you need to specify the type of parameter for the key—the enum type—and pass the constructor an object that describes the key type.
In this case, the key type is Type, and the object is Type.class.

15.
What will be the result of running the code below?
class WeakKey {}
public class WeakHashMapDemo  {
    public static void main(String[] args) throws InterruptedException {
        Map<WeakKey, String> weakMap = new WeakHashMap<>();
        WeakKey key = new WeakKey();
        WeakKey key1 = new WeakKey();
        WeakKey key2 = new WeakKey();
        weakMap.put(key, "s0");
        weakMap.put(key1, "s1");
        weakMap.put(key2, "s2");
        System.out.print(gcAndSize(weakMap));
        weakMap.put(null, "s");
        System.out.print(" " + gcAndSize(weakMap));
        key1 = key2 = null;
        System.out.print(" " + gcAndSize(weakMap));
    }
    static int gcAndSize(Map<WeakKey, String> map) throws InterruptedException {
        System.gc();
        Thread.sleep(1000);
        return map.size();
    }
}
3 4 2
The WeakKey class inherits from the Object class.
Its objects can be created and used as keys in a map.
Null keys can be added to a WeakHashMap.
When external key references are no longer used, such keys will be removed from the map by the garbage collector.

16.
What will be the result of running the code below?
class WeakKey {}
public class WeakHashMapDemo {
    public static void main(String[] args) throws InterruptedException {
        Map<WeakKey, Integer> weakMap = new WeakHashMap<>();
        WeakKey key1 = new WeakKey();
        WeakKey key2 = new WeakKey();
        weakMap.put(key1, 11);
        weakMap.put(key2, 21);
        Iterator<WeakKey> iterator = weakMap.keySet().iterator();
        System.out.print(gcAndSize(weakMap));
        key1 = null;
        System.out.print(" " + gcAndSize(weakMap));
        System.out.print(" " + iterator.hasNext());
    }
    static int gcAndSize(Map<WeakKey, Integer> map) throws InterruptedException {
        System.gc();
        Thread.sleep(1000);
        return map.size();
    }
}
2 1 true
When you get an iterator to iterate over the keys, the WeakHashMap contains two entries.
The map then changes structurally as one of the keys is discarded since it is not used outside the collection.
The keys set also changes, leaving only one element.
However, the iterator on it works correctly because the keys set is a hash table (a one-dimensional array), the reference to which remains unchanged.
Therefore, calling the hasNext() iterator method returns true.

17.
Which of the following statements about the legacy collections are true?
All these collections are synchronized, which makes them slower compared to modern collections.
The Vector<E> class is not memory efficient because it automatically doubles the size limit when it is reached.
Legacy collections were originally created synchronized for their use in multithreaded applications, but they were also used in single-threaded applications.
Synchronization makes applications slower.
The Vector‹E› class is simply a list implemented in an array that grows in size without regard to the possible increase in the number of elements.

18.
Which statements about the differences between legacy and modern collections are true?
The Hashtable<K,V> collection does not allow null keys and null values, unlike HashMap<K,V>.
Both the Vector<E> class and the ArrayList<E> class use the Iterator<E> interface for iteration.
A collection of the Stack‹E› type is iterated to the top of the stack and also allows access to elements by index.
Both Vector‹E› and ArrayList‹E› can use the Iteration‹E› interface to iterate over elements.
The Hashtable‹K,V› collection does not allow null keys and null values.

19.
What will be the result of executing the following code?
import java.util.Enumeration;
import java.util.Stack;
public class LegacyDemo {
    public static void main(String[] args) {
        Stack<Number> stack = new Stack<>();
        stack.push(0);
        stack.push(10);
        stack.push(20);
        stack.add(30);
        stack.add(2, 25);
        Enumeration<Number> iter = stack.elements();
        while (iter.hasMoreElements()) {
            System.out.print(iter.nextElement() + " ");
        }
    }
}
0 10 25 20 30
The Stack‹E› collection allows you to add objects with the push method to the top of the stack and the add method to an existing position. 
An iterator of type Enumeration‹E› iterates over the elements of the stack from the first element placed on it to its top.
Since the numbering starts from 0, then the value 25 will be the third in order from the beginning.
So the result is 0 10 25 20 30

20.
Which of the following statements about the Hashtable<K,V> class are true?
Hashtable<K,V> is synchronized, while a HashMap<K,V> is not.
Hashtable<K,V> cannot have null values or null keys.
The Hashtable‹K,V› class is synchronized and cannot use null as a key or value, unlike its newer HashMap‹K,V› version. 
Also, both of these collections store elements unordered and their behavior does not depend on the garbage collector.

21.
Which of the following implements or inherits the Properties class?
Hashtable
The Properties class only inherits the Hashtable class because the properties are stored as a hash map.

22.
What is the result of compiling and running the code below considering the contents of the base.properties file given?
public class PropertiesQuest1 {
    public static void main(String[] args) throws IOException {
        Properties props = new Properties();
        props.load(new FileReader("base.properties"));
        String key1 = props.getProperty("key1");
        String key2 = props.getProperty("key2");
        String key3 = props.getProperty("key3");
        System.out.print(" key1:" + key1);
        System.out.print(" key2:" + key2);
        System.out.print(" key3:" + key3);
    }
}
File base.properties
key1=n
key2=o
key2=w
key4=a
key5=r
key1:n key2:w key3:null
For the property named "key1", we get the value "n". 
For the "key2" property, we get the value "w" because if the properties' names in the file match,
there will be only one pair in the Properties object with this name and with the last value specified for it. 
We get null for the property named "key3" because this property does not exist in the file.

23.
What is the result of compiling and running the code below considering the contents of the base.properties file given?
public class PropertiesQuest2 {
    public static void main(String[] args) throws IOException {
        Properties props = new Properties();
        props.load(new FileReader("base.properties"));
        Set<Object> set = props.keySet();
        System.out.println(set.size());
    
    }
}

File base.properties
key1=n
key2=o
key2=w
key4=a
key5=r
4
The file describes 5 properties, two of which have the same name—key2.
When loading the properties file, there will only be four elements in the Properties object because the internal properties are stored as a hash table, 
which does not allow duplicate keys.

24.
Which interface implemented in TreeSet<E> will allow for natural element ordering?
java.lang.Comparable
The implementation of the Comparable<T> interface by classes allows you to define a natural sort order for them. This interface is located in the java.lang package.

25.
Which of the following methods does the Comparable<T> interface define? 
compareTo
The Comparable<T> interface has one abstract method defined—compareTo().

26.
What will be the result of executing the following code?
import java.util.*;
public class ComparatorQues1 {
    public static void main(String[] args) {
        ArrayList<Integer> arrayList = new ArrayList<>();
        arrayList.add(20);
        arrayList.add(10);
        arrayList.add(30);
        arrayList.add(0);
        arrayList.sort(new Comparator<Integer>() {
                @Override
                public int compare(Integer o1, Integer o2) {
                    return o2 - o1;
                }
        });
        System.out.println(arrayList);
    }
}
[30, 20, 10, 0]
The Integer class implements the Comparable<T> interface and defines a natural ascending sort order. 
To define reverse sorting, the Compataror<T> interface is used. It is implemented through an anonymous class.

27.
What will be the result of executing the following code?
import java.util.*;
public class Student {
    private String name;
    private int group;
    public Student(String name, int group) {
        if (name == null> || group <= 0) {
            throw new IllegalArgumentException();
        }
        this.name = name;
        this.group = group;
    }
    public String toString() {
        return "Student: name=" + name + " group=" + group;
    }
    public int compareTo(Student o) {
        int groupCmp = this.group - o.group;
        return (groupCmp != 0) ? groupCmp : this.name.compareTo(o.name);
    }
}

public class StudentComparableQues2 {
    public static void main(String[] args) {
    Student[] students = {
        new Student("Ivan", 501),
        new Student("Nina", 101),
        new Student("Alex", 501),
        new Student("Lilu", 301),
        };
        List<Student> list = Arrays.asList(students);
        Collections.sort(list);
        System.out.println(list);
    }
}
Compilation fails
The Student class does not implement the Comparable<T> interface as the compareTo() method is its own method. 
So, the argument of the Collections.sort() method does not match and cannot be cast to the Comparable<T> type.

28.
Which statements about the Collections class are true? 
The Collections class contains methods that wrap the specified collection and return a new one with certain features.
The Collections class consists exclusively of static methods that operate on or return collections.
The Collections class contains only static methods that implement various algorithms that process collections or wrap them in collections with certain properties. 
Most of the methods are generic and work with any type of collection. 
Also, all methods throw an exception of type NullPointerException if the collections or class objects provided to them are null.

29.
What statements about the Collections class methods are true?
The sort() method implements a merge sort algorithm that has the stability property.
The binarySearch() method works correctly only in an ordered list.
The sort() method implements a merge sort algorithm that works steadily and has two forms for ordering: natural order and according to the passed comparator. 
The binarySearch() method requires the collection to be ordered before executing it. Only the shuffle() method can be applied to lists.

30.
What will be the result of executing the following code?
import java.util.*;
public class CollectionsMethods {
    public static void main (String[] args) {
        List<Integer> list = new ArrayList<>();
        Collections.addAll(list, 100, -100, 50, -50, 0);
        Collections.sort(list);
        Collections.swap(list, 3, 4);
        int index = Collections.binarySearch(list, 50);
        System.out.println(index);
    } 
}
Unpredictable output
The sort() method sorts the list of numbers in ascending order by default. Therefore, the value 50 will be in position 3. 
However, after swapping this element with the one following it, the binarySearch() method will no longer work correctly and its result is unpredictable. 
No errors occur because the method returns some negative value if the object it is looking for is not found.


